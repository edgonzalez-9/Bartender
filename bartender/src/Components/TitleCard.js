import React from 'react';

const TitleCard = props => {
    return(
        <div style={{backgroundColor: `rgba(0,0,0, .5)`, padding: `15px`}}>
            <h1 class="title">Bar Cart</h1>
        </div>
    )
}

export default TitleCard;